import React from 'react';

import { Face } from '../../components/Face';

export const AboutMe = () => {
  return (
    <div
      style={{
        display: 'flex',
        justifyContent: 'center',
      }}
    >
      <Face />
    </div>
  );
};
